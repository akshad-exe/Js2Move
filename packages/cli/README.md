# MoveJS CLI

Minimal CLI for MoveJS compiler tasks.

Commands (initial):
- `movejs compile` — compile a single file or inline source to Move
- `movejs build` — compile all `.movejs` files in the current directory

Dev:
- `pnpm --filter @js2move/cli dev`
- `pnpm --filter @js2move/cli build`