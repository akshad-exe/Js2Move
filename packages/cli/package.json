{
  "name": "@js2move/cli",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "movejs": "dist/index.js"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest",
    "lint": "eslint 'src/**' --fix"
  },
  "dependencies": {
    "commander": "^10.0.0",
    "@js2move/compiler": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "ts-node-dev": "^2.0.0",
    "vitest": "^1.0.0",
    "@types/node": "^18.0.0"
  }
}