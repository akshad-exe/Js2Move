import { IR } from '../ir';

/**
 * Generate Move source code from IR.
 * For now this is a placeholder that returns a small comment and a stub function.
 */
export function generateMove(ir: IR): string {
  return `// Generated Move code (stub)
`;
}
