// Test control flow statements
contract ControlFlow {
  check(value: u64): bool {
    if (value > 100) {
      return true;
    } else {
      return false;
    }
  }
  
  loop_test(n: u64) {
    while (n > 0) {
      n = n - 1;
    }
  }
}
